% load data
% addpath('matlab','data')
% load('calcium_data.mat')
% load('stimuli_info.mat')

% data summary
num_fields = 8;
dat_summary = get_info(data,stimuli_info, num_fields);

% figure(1)
% dat = data{1}.ROI7.movie_1.dff;
% plot(dat)
% 
% dat_deriv = diff(data{1}.ROI7.movie_1.deNoise_dff, 1, 2);
% z = zscore(dat_deriv);
% figure(2)
% plot(z)
% 
thrld = 0.25;
fov = 1;
stim_num = 1;
events_stim = get_spikes_stim(dat_sorted,fov,thrld,stim_num);

mymap = [0 0 1; 1 0 0];
heatmap(events_stim, 'Colormap', gray)